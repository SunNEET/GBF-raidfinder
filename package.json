{
  "name": "nepu-friends",
  "version": "0.9.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3",
    "popper.js": "^1.14.4",
    "socket.io": "^2.1.1",
    "twitter": "^1.7.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "devDependencies": {
    "node-loader": "^0.6.0"
  }
}
